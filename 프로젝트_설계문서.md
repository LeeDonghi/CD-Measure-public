# CD 측정 사진 → 엑셀 변환 프로그램 (설계 문서)

> 이 문서는 프로젝트를 이어받는 사람(또는 Claude Code)을 위한 안내서입니다.
> 사용자는 **코딩 초보인 반도체 photo 엔지니어**입니다. 설명은 쉽게, 단계별로 해주세요.
> 사용자 환경: **윈도우 + VS Code + Python**. 프로젝트 폴더명: `CD Measure`.

---

## 1. 이 프로그램이 하려는 일 (한 줄 요약)

측정 장비가 저장한 **사진(.bmp)** 들을 폴더째 읽어서, 사진 안에 찍힌 측정값을
**OCR로 읽어내 엑셀로 정리**하는 프로그램. (장비는 사진만 저장하고, 별도 데이터
파일은 남기지 않음 — 그래서 OCR이 유일한 방법.)

---

## 2. 측정 워크플로우 (도메인 지식 — 매우 중요)

- Wafer 하나에 여러 **Point**를 측정한다.
- **한 Point당 사진 6장**이 시간 순서로 저장된다.
- 6장은 아래 3종류의 측정에 대해 각각 **(측정 전 사진 + 측정값 사진)** 2장씩 = 6장:
  1. **overlay** (측정 전 / 측정값)
  2. **14.5um CD** (측정 전 / 측정값)  ← 14.5는 Target(목표)이고 실제 측정값은 다름
  3. **9.5um + 5.5um Line/Space (L/S)** (측정 전 / 측정값)  ← 9.5/5.5도 Target
- **측정 전 사진은 버린다** (측정값이 안 찍혀 있어 쓸모없음).
- **순서는 뒤섞일 수 있다.** overlay가 먼저일 수도, L/S가 먼저일 수도 있음.
  측정 전/측정값 순서도 항목마다 다를 수 있음. → 순서에 의존하면 안 됨.

### 파일명 규칙
- 파일명은 `CaptImg + 시간(YYYYMMDDHHMMSS).bmp` 형식.
  예: `CaptImg20260619110318.bmp`
- **파일명에는 종류 힌트가 없다.** (overlay인지 CD인지 파일명으로 알 수 없음)
- 시간 순서 정렬은 파일명으로 가능 (시간이 파일명에 들어있음).

---

## 3. 검증 완료된 핵심 규칙 (18장으로 테스트함)

### 규칙 A — 측정 전/측정값 사진 구분 (검증 완료 ✓)
- **측정값 사진**: 흰색 글자로 측정값이 찍혀 있음 (`번호=값,XY=(X,Y)um` 형식).
- **측정 전 사진**: 흰색 측정값 글자가 없음.
- → **흰색 텍스트가 있으면 사용, 없으면 버림.** 18장 중 정확히 9장/9장으로 갈렸음.

### 규칙 B — 측정 종류 구분 (논리 검증 완료 ✓)
측정값 사진에서 **값의 개수**로 종류를 판별:
- **값 4개** (번호 0,1,2,3, 각각 `XY=(X,Y)`) → **overlay** (십자 마크 사방 측정)
- **값 1개** → **14.5um CD** (라인 폭 하나 측정)
- **값 2개** → **9.5+5.5 L/S** (라인 폭 + 스페이스 폭)

18장 → 측정값 9장이 overlay 3 / CD 3 / L/S 3 으로 정확히 나뉨.

### 규칙 C — Point 묶기
- 시간 순 정렬 후, 측정값 사진만 남기면 Point당 3장(overlay/CD/L/S).
- 3장이 한 세트 = 1 Point. (또는 원본 6장을 6개씩 끊어도 됨)
- 안전장치: 한 세트에 overlay/CD/L/S가 하나씩 다 있는지 확인. 없으면 경고.

---

## 4. 미해결 과제 (Claude Code가 이어서 할 일)

### 과제 1 — OCR 숫자 정확도 (해결됨 ✓ — 2026-07-21)
~~Tesseract가 특정 숫자를 오독함~~ → **Tesseract를 버리고 템플릿 매칭 방식으로 교체해서 해결.**

**핵심 아이디어:** 이 계측기 사진은 글자 폰트·색상이 항상 고정됨. 그래서 범용 OCR로
애매하게 "읽으려" 하는 대신, 이미 정답을 확인한 숫자 모양(템플릿)과 새 글자를
한 개씩 잘라 비교해서 제일 비슷한 걸 고르는 방식을 씀 (패턴 매칭과 같은 원리).

- 확인됐던 오독 원인 (Tesseract 한정):
  - 한 줄 전체("2=6.8,XY=(0.0,6.8)um")를 통째로 읽으려다 실패 → `6`을 `8`로,
    `5`를 `8`로 오독하는 등 (배율을 키워도 해결 안 됨, 폰트 자체를 헷갈림)
  - 앞부분이 통째로 깨지면, 코드가 대신 괄호 안 좌표값을 엉뚱하게 집어오는
    2차 버그도 있었음 (예: `3=6.5,XY=(0.2,6.5)` → `6.5` 대신 `0.2`를 읽음)
- **새 방식 (템플릿 매칭):**
  1. `build_templates.py` — 정답을 눈으로 확인한 사진 몇 장에서 0~9 숫자와
     기호(`=`,`,`,`.`,`(`,`)`,`X`,`Y`) 모양을 글자 단위로 잘라 `char_templates.pkl`에 저장.
  2. `ocr_core.py` — 새 사진에서 글자를 하나씩 잘라 템플릿과 비교(픽셀 일치율)해서
     제일 비슷한 글자를 고르고, `번호=값,XY=(X,Y)` 형식으로 조립. 매칭 점수가 낮으면
     `확인필요=True`로 표시 (신뢰도 임계값 0.85, `ocr_core.CONFIDENCE_THRESHOLD`).
  3. `ocr_test.py` — 위 모듈을 사용하도록 교체 완료.
     (※ 이 파일은 2026-08-04에 삭제됨 — 아래 "삭제한 파일" 항목 참고)
- **검증 결과:** Sample 폴더 사진 72장(측정값 있는 39장) 전체 재테스트.
  - CD(목표 14.5): 예전엔 12.8~18.3까지 널뛰던 값이 → 14.6~15.3으로 안정화
  - L/S(목표 9.5+5.5): 튀는 값(예: 8.7) 사라지고 → 5.2~5.7 / 9.2~9.6로 안정화
  - overlay: 좌표 오추출(`0.2` 등) 완전히 사라짐
  - "확인필요"로 걸린 값 0개 (39장 전부 신뢰도 0.85 이상)
- **남은 다듬을 거리 (급하지 않음):**
  - 템플릿이 사진 6~7줄에서만 뽑혀서 숫자별 예시 수가 적음(`3`은 1개, `4`,`8`,`9`는 2개뿐).
    나중에 오독이 발견되면 `build_templates.py`의 `KNOWN` 목록에 사례를 추가하고 재실행.
  - 메인 프로그램(`CD측정값_엑셀변환.py`)은 아직 예전 Tesseract 방식 그대로임 —
    이 프로그램에 새 `ocr_core.py`를 연결하고, 측정 전/후 구분·종류 판별·Point 묶기까지
    합치는 작업이 남음.

### 과제 2 — overlay 4개 값의 방향 매칭 (해결됨 ✓ — 2026-07-22)
- overlay는 십자 마크의 **위/아래/좌/우** 4방향 값. 방향이 의미가 있음.
- OCR이 읽는 순서(번호) ≠ 사진 속 위치 순서. → **글자 덩어리의 화면 좌표(pos_x, pos_y)**를
  4점의 중심과 비교해 상/하/좌/우를 판별 (`overlay_analysis.py`의 `assign_directions`).
- `Overlay_X = |좌-우|/2`, `Overlay_Y = |상-하|/2`, `Overlay = sqrt(Overlay_X²+Overlay_Y²)`로
  정의하고 `compute_overlay_metrics`에서 계산. Excel `Overlay요약` 시트 + HTML 리포트에 반영됨.

### 과제 3 — 값 검증(신뢰도) 장치 (해결됨 ✓ — 2026-07-25, 값-좌표 정합성 추가)
- OCR 신뢰도가 낮은 값은 엑셀 "확인필요" 열에 표시함(`ocr_core.CONFIDENCE_THRESHOLD=0.85` 기준).
- **값·좌표 교차검증 반영 완료 (2026-07-25)**: 한 줄의 세 숫자(값, X, Y)를 실제 사진
  113줄로 분석한 결과 **`값 = √(X² + Y²)`**(값 = 좌표 벡터의 길이)임을 발견. 값과 좌표는
  줄에서 따로 찍혀 독립적으로 읽히므로, 이 관계가 허용 오차(`REDUNDANCY_TOLERANCE=0.05`)보다
  크게 깨지면 `확인필요=True`로 표시함(`ocr_core.py`의 `read_value_line`). 값 자체는 안 바꾸고
  표시만(과제 4 밝기검증과 동일 철학).
- **검증 결과**: 113줄 중 정상 112줄은 오차 0.012 이하로 통과, 1줄(`8 (45).bmp` 번호1,
  값=1.8 vs 좌표 길이 1.71, 오차 0.088)만 걸림 — 오탐 0건. 이 1줄은 사진 실물을 잘라 확인한
  결과 **OCR은 정상**이었고(사진에 `1=1.8,XY=(0.2,1.7)` 그대로 찍혀 있음), 계측기가 값과
  좌표를 서로 안 맞게 출력한 **원본 데이터 불일치**였음. 즉 이 검증기는 "OCR 오독 검출기"가
  아니라 **"값-좌표 정합성 검증기"**(OCR 오독 + 원본 불일치 둘 다 잡음)로 이해해야 정확함.
- **아직 안 한 것(선택)**: ②Target 대비 물리적 타당성, ③같은 Item 내 이상치, ④번호 연속성
  같은 추가 교차검증은 미구현. 급하지 않음(①만으로 과제 3 취지는 충족).

### 과제 4 — Line/Space 배정 신뢰도 (해결됨 ✓ — 2026-07-25)
- L/S는 두 값 중 어느 게 Line/Space인지 **Target과 가까운 쪽으로 자동 판별**함
  (`measurement_plan.py`의 `match_ls_targets`). 그런데 **Line Target == Space Target**이면
  오차가 항상 같아져서(0=0 tie) 배정이 사실상 랜덤해짐 — 실제 `LS Sample` 폴더(Line=Space=2um
  테스트용, 사용자가 직접 촬영해 추가)로 재현/검증함. 사진 속 "번호" 순서나 화면 위치(pos_y)도
  사진마다 뒤바뀌어서(2026-07-24 확인) 신뢰할 수 없음 — 순서 기반 판별은 불가능.
- **유력한 대안을 찾음 (검증됨, 아직 코드 미반영):** 사진은 **도금 전** 사진이라, Line은
  PR이 없어 바닥면이 보여 **어둡게**, Space는 PR이 덮여 있어 **밝게** 찍힘. 크로스헤어(초록색
  마커) 주변 픽셀 밝기를 비교했더니 `LS Sample` 6장 전부 "큰 값=어두움=Line, 작은 값=밝음=
  Space" 패턴이 일관됐고, 번호 순서가 뒤집힌 문제 사진에서도 밝기 패턴만은 안 뒤집혔음
  (사용자가 직접 원리 확인·확정함: "PR 없는 바닥면=어둡게, PR 있으면 밝게").
- **반영 완료 (2026-07-25)**: `measurement_plan.py`의 `match_ls_targets`에서 Target이 같아
  오차가 tie(err_a==err_b)가 되면, 두 값 중 **큰 쪽을 Line, 작은 쪽을 Space**로 배정(1차 원칙 —
  Line이 Space보다 물리적으로 큼). 새 파일 `ls_brightness.py`(`verify_ls_brightness`)가 크로스헤어
  자동 검출 + 주변 밝기 비교로 이 배정이 맞는지 2차 검증하며, Target이 같을 때만 실행됨
  (`CD측정값_엑셀변환.py`의 `process_folder`에서 호출). 크로스헤어가 밝기 경계에 걸치는 사진이
  있어 100% 일치하진 않음(`LS Sample` 11장 중 9장 일치) — 그래서 **배정을 뒤집지 않고**, 불일치
  시에만 로그 + 해당 행 `확인필요=True`로 표시함. (버그 노트: 첫 구현에서 `verify_ls_brightness`가
  `np.False_`를 반환해 `is False` 비교가 통과 안 되는 문제가 있었음 → `bool()`로 감싸서 해결.)
- **별개로 발견한 위험**: `LS Sample` 폴더의 파일명이 `1 (45).bmp`, `10 (43).bmp`... 형식이라
  문자열 정렬 시 시간 순서가 깨짐(1,10,11,...,2,3 순으로 꼬임). 지금 프로그램(`main()`)은
  파일명 문자열 정렬(`sorted(set(files))`)만 쓰므로, 장비가 이런 파일명 규칙을 쓰면 Point
  묶기(3장씩)가 깨질 수 있음 — 실제 장비 파일명 규칙 확인 필요.

### 과제 5 — OCR 강건성: 겹친 측정줄 / XY 붙음 / 밝은 패드 오인식 (해결됨 ✓ — 2026-07-28)
`Sample2`/`Sample3` 폴더(실제 문제 사진, 사용자가 촬영해 추가)로 재현·검증함.

- **겹쳐 그려진 측정줄**: 계측기가 측정줄 두 개를 화면에 겹쳐 그리면 글자가
  물리적으로 뭉개져서 그 줄은 아예 못 읽음. 문제는 "읽은 값 개수"로 종류(overlay/
  CD/L·S)를 판별하면, 4줄짜리 overlay 사진이 2줄만 읽혀서 Line&Space로 둔갑해
  엉뚱한 Target에 매칭되던 것. → **박스 폭으로 "실제 몇 줄이었는지" 추정**하는
  `count_lines_in_boxes`를 추가해서, 종류 판별은 읽은 개수가 아니라 이 추정
  줄 개수로 하도록 바꿈(`ocr_core.read_image_ex`). 값이 뭉개져 못 읽은 줄이
  있으면 그 사진에서 읽힌 나머지 값도 전부 `확인필요=True`로 표시하고, 완료
  안내창에 "N장에서 측정값 일부를 못 읽었어요"라고 알려줌
  (`CD측정값_엑셀변환.py`의 `UNREAD_MARK`, `process_folder`).
  - 글자줄 묶기용 가로 부풀림도 ±14 → ±5로 줄임. 예전 ±14는 나란히 찍힌
    다른 측정줄까지 한 덩어리로 붙여버려 그 줄 전체를 못 읽는 원인이었음
    (`LS Sample`의 `7 (43).bmp` 등에서 확인).
  - 밝은 사진은 배경까지 흰색(글자 마스크)으로 잡혀 12만 픽셀씩 잡히는 문제가
    있어, 흰 픽셀 비율이 기준(1.5%)을 넘으면 밝기 임계값을 단계적으로
    올리도록 자동 조절(`get_text_mask`).
- **XY 붙음**: `X`와 `Y`가 화면에서 맞닿아 찍히면 글자 하나로 뭉개져 엉뚱한
  글자(주로 `7`)로 읽히는데, 예전엔 이때 형식이 안 맞는다고 줄 전체를 버렸음
  (`Sample3`에서 14장 중 12장을 통째로 놓침). → 값과 좌표 사이 `,XY=` 부분은
  글자 그대로 요구하지 않고 "아무 글자 3개까지"로 완화(`ocr_core.LINE_RE`).
  잘못 읽을 위험은 과제 3의 `값=√(X²+Y²)` 교차검증이 잡아줌(느슨하게 풀어도
  안전한지 4개 폴더 전수 비교로 확인함).
  - 신뢰도 점수도 이 부분(`,XY=`) 글자는 안 보고, **실제로 값에 쓰는 글자
    (번호·값·X좌표·Y좌표)만** 보고 판단하도록 수정 — 안 그러면 `,XY=`가
    뭉개졌다는 이유만으로 값이 멀쩡한 줄까지 무더기로 "확인필요"에 걸려
    진짜 이상값이 묻힘.
- **밝은 패드 오인식**: 밝은 원형 패드 무늬가 흰색 마스크로 잡히면 세로
  180px짜리 큰 덩어리가 생겨 글자줄로 오인됨. 실측 결과 측정줄 한 줄의
  높이는 항상 19px, 두 줄이 겹쳐도 28px을 안 넘음(4개 폴더 187줄 전수 확인)
  → 세로 40px 넘는 덩어리는 글자줄에서 제외(`MAX_LINE_HEIGHT`).

### 과제 6 — JPG/JPEG 지원 (해결됨 ✓ — 2026-07-28)
장비가 `.bmp` 대신 `.jpg`/`.jpeg`로 저장하는 경우도 지원(`IMAGE_EXTENSIONS`에
추가). 단 JPG는 손실 압축이라 글자 경계가 뭉개져 OCR 오독 위험이 BMP/PNG보다
큼 — 프로그램 실행 시 안내 문구로 사용자에게 알림. 이미 확인필요/교차검증
장치가 있어 완전히 막지는 않아도 걸러지긴 함.

### 과제 7 — 밝은 자국에 붙는 글자 (해결됨 ✓ — 2026-08-07)
`9-1_22.bmp` 한 장이 라벨은 깨끗한데 값을 하나도 못 읽었음(`Pad CD 예상 9 / 실제 8`).
원인은 **글자 아래의 흐릿한 밝은 자국**(가로줄당 흰 픽셀 2~7개)이 밝기 기준 170에
같이 잡히는 것. 이게 두 가지를 동시에 망가뜨림 — 줄박스 높이가 19px에서 27px로
부풀고(`_char_bitmap`이 그 높이를 16×24로 줄이므로 **글자가 세로로 눌려** 템플릿과
안 맞게 됨), 글자 사이 빈칸이 메워져 **가로 분해가 어긋남**(8+7+2px가 17px 한
덩어리로 붙음).

**해결: 세로 다듬기 + 가로 분할, 둘 다 있어야 함**(`b124aeb` → `98cdc64`).
박스에서 잉크가 거의 없는 행을 잘라낸 뒤에야 세로 투영 골짜기 분할이 통했음.
가로 분할만 먼저 넣었을 땐 **개선이 정확히 0이라 실패로 보고 되돌렸었음**
(`afecc81` → `288352e`) — 분할은 멀쩡했고 그 앞 단계인 줄박스가 이미 망가져
있었던 것. 글자폭 상한 13px은 추측이 아니라 **글자 6355개 전수 조사**로 정한 값
(99.3%가 13px 이하).

**다시 시도하지 말 것 — Sauvola·Otsu 등 문서 이진화 계열.** 파라미터 27조합을
훑어도 점수가 전혀 안 변하고 최소밝기만 영향을 줬음 = Sauvola 항이 아무 역할을
못 함(최고 2/5, 지금 방식은 4/5). 이유가 원리적임: 이 계열은 "흰 종이 위 검은
글자"용이라 주변 통계가 기준선이 되어주는데, 우리 사진은 **"사진 위에 계측기가
덧그린 흰 글자"**라 주변이 패드·배경이라 기준선이 없음.
**밝기 기준을 올리는 것도 답이 아님** — 180은 통과하지만 210은 획이 깎여 1을 4로
오독함(값 400.2, 신뢰도 0.888). 채도 조건 추가도 잘 읽히던 사진을 망가뜨림.

우리 글자의 진짜 특성은 "밝다"가 아니라 **"소프트웨어가 그려서 기하학적으로
정확하다"**(높이 19px 고정, 폰트·글자폭 고정)는 것 — 이진화가 아니라 글자 분해
쪽을 손대는 게 맞다.

> 실험 표와 행별 잉크 측정 같은 경위는 개발 기록(비공개 저장소 `작업일지.md`의
> 2026-08-04 절 "🔍 OCR 문제 2건")에 남겼다. 이 문서에는 결론만 옮겼다.

---

## 5. 프로그램이 사용자에게 물어봐야 할 것 (해결됨 ✓ — 계속 확장 중, 최신 2026-07-24)

**GUI 입력 폼으로 구현 완료.** 프로그램 실행 시 tkinter 창이 5단계로 뜸 (`plan_gui.py`):
0. **고객 / 자재 / Layer** 입력 (2026-07-24 추가 — HTML 리포트 제목에 사용됨)
1. 공정 선택: RDL / PI / Bump / **Cu Post** (2026-07-22 Cu Post 추가)
2. 공정별 CD 종류(RDL=Pad CD, PI=Via CD, Bump=UBM CD, Cu Post=Cu Post CD) — Item 개수 입력 →
   Item별 "측정 포인트 수" + "Target 값" 입력칸 자동 생성. **Tolerance는 회사 기준표
   (`tolerance_table.py`)에서 Target 값으로 자동 조회**됨 (사용자가 직접 입력 안 해도 됨).
3. RDL인 경우 Line&Space도 추가로 입력 (Item별 포인트 수 + Line/Space Target 각각,
   Tolerance는 Line/Space 각각 독립적으로 자동 조회)
4. Overlay 측정 포인트 수 입력 → 폴더 선택(대화상자)까지 하면 바로 처리 시작

**Target 매칭 방식:** 사진 순서로 값↔Item을 딱 맞추지 않음 (순서가 뒤섞일 수 있어서 위험).
대신 측정값마다 **제일 가까운 Target을 가진 Item**을 찾아 편차를 계산함
(`measurement_plan.py`의 `match_cd_target`/`match_ls_targets`). L/S는 두 값 중 어느 쪽이
Line/Space인지도 오차가 작은 배정으로 자동 판별 — **단, Target이 같으면 판별 불가 (과제 4 참고)**.

**검증 요약:** Item마다 "예상 포인트 수"(입력값) vs "실제 매칭된 개수"를 비교해
엑셀의 "검증요약" 시트에 표시 — 측정 누락/초과를 바로 알 수 있음.

**Cpk/통계 (해결됨 ✓ — 2026-07-22):** Item별 N/Avg/Std(표본표준편차, 엑셀 STDEV()와 동일)/
Max/Min/Range/USL/LSL/Cp/Cpk를 `compute_stats()`로 계산해 엑셀 "Cpk요약" 시트(조건부 서식+
막대그래프) + "트렌드차트" 시트(Item별 Point 추이 선그래프)에 반영. 표시 이름은
"Target값 종류" 형식(예: `14.5 Pad CD`, `9.5 Line`)으로 통일 (`display_item_name()`).

**HTML 대시보드 리포트 (해결됨 ✓ — 2026-07-23~24):** 엑셀과 별개로 `측정결과.html`을
`html_report.py`에서 생성 — 외부 라이브러리/인터넷 없이 순수 SVG로 그린 KPI 타일, Item별
CPK 카드(색상 상태 표시), 막대/추이 그래프, Overlay 요약을 담음. 폴더째 다른 PC로 옮겨도
그대로 열림. 제목은 "{고객} {자재} {Layer} Photo 측정 결과 보고서" 형식.

**3σ/CDU%/판정 지표 (해결됨 ✓ — 2026-07-28):** 엑셀 "Cpk요약" 시트와 HTML 카드/통계표에
Item별 3σ, CDU%(3σ/Avg×100), Overall 판정(양호/주의/불량 등, 색칠 포함) 열을 추가
(`41a6667`, `7c1fb30`). 통계표 표준편차 열 이름은 엑셀 STDEV()와 헷갈리지 않게
"Std (1σ)"로 표기 통일.

**결과 파일 자동 열기 (해결됨 ✓ — 2026-07-28):** 처리 완료 안내창을 먼저 띄운 뒤(경고
문구를 놓치지 않도록), 확인을 누르면 `측정결과.html`과 `측정결과.xlsx`를 자동으로 엶.
폴더명에 `#`이나 공백이 있어도 정상적으로 열리도록 경로 정규화 + `cmd start` 2차 시도까지
보완(실패해도 창으로 안내, 조용히 실패하지 않음).

**실행 로그 저장 (해결됨 ✓ — 2026-07-29):** 실행마다 사진 폴더 안에 상세 로그
(`측정로그_시각.txt`, 뭘 읽고 뭘 버렸는지 등)를 저장하고, 프로그램 폴더의
`실행이력.csv`에 한 줄 요약(날짜·고객·자재·Layer·총 측정값·스펙이탈·확인필요 개수 등)을
누적함. `실행이력.csv`는 여러 Lot을 넘나드는 누적 데이터라 사진 폴더가 아니라
프로그램 폴더에 고정, git으로 추적함(지우면 안 됨).

**새 파일:** `measurement_plan.py`(데이터 구조+매칭+통계), `plan_gui.py`(입력 폼),
`tolerance_table.py`(Tolerance 자동조회), `overlay_analysis.py`(방향판별), `html_report.py`
(HTML 리포트). GUI라 Claude가 직접 클릭 테스트는 못 하고, 로직만 검증함 — 실제 사용성은
사용자가 직접 실행해서 확인.

### 엑셀 출력 (구현 완료)
- 파일명 / Point 번호 / 측정 종류 / 측정값 / X / Y / **Target 대비 편차**
- 스펙(USL/LSL) 벗어난 값 빨간 색칠(`스펙이탈` 열), OCR 의심값 "확인필요" 표시
- 시트: 측정결과 / 검증요약 / Overlay요약 / Cpk요약 / 트렌드차트

---

## 6. 지금 있는 파일 (2026-08-01 기준)

- `CD측정값_엑셀변환.py` — **메인 프로그램**. GUI로 계획 입력받고, 사진 폴더를 읽어
  Point 묶기 + Target 매칭 + 스펙 판정까지 해서 `측정결과.xlsx` + `측정결과.html`을 생성.
- `ocr_core.py` — OCR 핵심 로직 (템플릿 매칭 방식). 글자 단위로 잘라 `char_templates.pkl`과
  비교해 인식. 신뢰도 낮으면 `확인필요=True`.
- `char_templates.pkl` / `build_templates.py` — 숫자·기호 템플릿 데이터 및 생성 스크립트.
- `overlay_analysis.py` — overlay 4값의 상/하/좌/우 방향 판별 + Overlay_X/Y/Overlay 계산.
- `measurement_plan.py` — 측정 계획 데이터 구조(`CDItem`/`LSItem`/`MeasurementPlan`),
  Target 매칭(`match_cd_target`/`match_ls_targets`), 통계/Cpk 계산(`compute_stats`),
  표시 이름 계산(`display_item_name`).
- `tolerance_table.py` — 공정/패턴별 Target→Tolerance 자동 조회 기준표.
- `ls_brightness.py` — L/S Target이 같을 때 크로스헤어 밝기로 Line/Space 배정 2차 검증(과제 4).
- `plan_gui.py` — tkinter 입력 폼 (고객/자재/Layer → 공정 선택 → Item/Target → Overlay
  → 폴더 선택).
- `html_report.py` — 로컬 HTML 대시보드 리포트 생성 (외부 라이브러리 없이 순수 SVG).
- `scripts/notify-toast.ps1` — 작업 완료 시 Windows 알림 팝업 스크립트 (매 응답마다 실행).
- `scripts/oracle_retry_launch.py` — **Oracle 무료 인스턴스 자동 재시도 생성**(2026-07-31
  신규). ARM(`A1.Flex`)이 기본, `--amd`로 AMD(`E2.1.Micro`, 2대까지)도 확보 가능.
  OCID를 하드코딩하지 않고 이름/조건으로 자동 조회하며, 개인 컴퓨터에서는 `~/.oci/config`,
  오라클 서버 안에서는 `--instance-principal`(키 파일 없이)로 인증. 확보 성공/중단 시
  디스코드 알림(`DISCORD_WEBHOOK_URL` 환경변수). 실행 로그 `scripts/oracle_retry.log`는
  git 추적 안 함.
- `scripts/oracle_grant_instance_permission.py` — 위 스크립트를 오라클 서버에서 돌릴 때
  필요한 **동적 그룹 + 정책**을 만들어 줌(2026-08-01 신규). 서버에 API 개인키를 두지
  않기 위한 것. 개인 컴퓨터에서 1회만 실행하면 되고, 여러 번 실행해도 안전.
- `scripts/oracle_recover_ssh_key.py` — **SSH 키를 잃은 서버 복구**(2026-08-04 신규,
  실제로 성공한 방법). 부트 볼륨을 접속 가능한 다른 서버에 데이터 볼륨으로 붙여
  `authorized_keys`를 직접 고침. 인스턴스를 종료하지 않으므로 서버·공인 IP를 잃지 않음.
- `scripts/deploy_to_server.py` — **웹앱 배포 자동화**(2026-08-04 신규).
  `webapp/배포방법.md` 2번 절차(포트 두 군데 열기 → Docker → 코드 업로드 → `.env` →
  `docker compose up`)를 순서대로 실행. `--domain` 필수.
- `scripts/oracle_add_ssh_key.py`, `scripts/oracle_bastion_session.py` — 키 교착을
  풀려고 만들었지만 **Ubuntu 이미지에서는 쓸 수 없음이 확인된** 방법들(Run Command·
  Bastion 플러그인이 에이전트에 아예 없음). 다른 OS(Oracle Linux 등)에서는 쓸 수
  있으므로 남겨두되, **이 서버들에는 다시 시도하지 말 것.**
- ~~`ocr_test.py`~~ — **2026-08-04 삭제함**(삭제한 파일). 프로젝트 초기 OCR 실험 코드로
  아무도 import하지 않았고, 무엇보다 **종류 판별이 "읽은 값 개수" 기반이라 현재 방식과
  어긋났음**(지금은 박스 폭으로 추정한 줄 개수 = `count_lines_in_boxes`. 겹쳐 찍힌 라벨
  때문에 바뀐 것). 낡은 방법을 보여주며 읽는 사람을 오해시켜서 지움.
  필요하면 `git log --diff-filter=D -- ocr_test.py`로 이력에서 복구 가능.
- `samples/Sample/` — 실제 측정 사진(RDL, CD=14.5um/L-S=9.5+5.5um 예시). Git에도 포함됨(Private repo).
- `samples/LS Sample/` — Line Target=Space Target=2um인 **엣지케이스 검증용** 사진(과제 4 참고).
  2026-07-24 사용자가 직접 촬영해 추가.
- `samples/Sample2/`, `samples/Sample3/` — 겹친 측정줄·XY 붙음 등 OCR 버그 재현용 사진.
  (2026-07-29에 네 사진 폴더를 최상위에서 `samples/` 밑으로 정리함)
- `실행이력.csv` — 실행마다 쌓이는 요약 로그(프로그램 폴더 고정, git 추적).
- `CLAUDE.md` — 매 세션 지켜야 할 작업 규칙(알림·커밋·회귀검증 기준 등). 배경/히스토리는
  이 문서와 아래 `집에서_이어서_작업하기.md`가 담당.
- `집에서_이어서_작업하기.md` — 다른 PC(집)에서 이어서 작업하기 위한 Git/알림 설정 안내.
- `webapp/` — **웹 버전** (2026-07-30~ 신규, 자세한 내용은 위 8번 섹션 참고):
  `app.py`(Flask 라우트, 로그인 잠금 포함), `users.py`/`add_user.py`(계정 관리),
  `Dockerfile`, `requirements.txt`, `배포방법.md`(실제 배포 명령어 모음),
  `templates/`(로그인/새측정/처리중/결과/실행기록/로그 화면). `webapp/users.json`·
  `webapp/uploads/`·`webapp/login_attempts.json`은 실행 중 생기는 데이터라 git
  추적 안 함(`.gitignore`).
- `docker-compose.yml`, `Caddyfile` — **외부 배포용 HTTPS 자동 처리**(2026-07-31
  신규). 웹앱 컨테이너 + Caddy(리버스 프록시)를 같이 띄워 Let's Encrypt 인증서를
  자동 발급/갱신함. `.env`(비밀값)와 `data/`(실행 데이터)는 git 추적 안 함.

---

## 7. 이어받는 사람에게

기본 뼈대(측정 전 걸러내기, 종류 구분, Point 묶기, OCR, Target 매칭, Cpk 통계, HTML 리포트)는
**실제 데이터로 검증되어 동작 중**임 — 믿고 써도 됨. GUI에 고객/자재/Layer 입력, Cu Post 공정,
Tolerance 자동조회까지 다 붙어 있어 사용자가 바로 실무에 쓸 수 있는 수준.

**과제 4 (Line/Space 배정)는 해결됨 (2026-07-25)** — Target이 같을 때는 크기(큰 값=Line)로
1차 배정하고, 크로스헤어 밝기로 2차 검증(불일치 시 로그+확인필요 표시, 배정 자체는 안 뒤집음).
**과제 3 (값·좌표 교차검증)도 해결됨 (2026-07-25)** — `값=√(X²+Y²)` 관계로 값-좌표 정합성을
검증해 안 맞으면 확인필요 표시(위 과제 3 항목 참고). 파일명 정렬 문제도 자연어 정렬로 처리
(`CD측정값_엑셀변환.py`의 `natural_sort_key`). **과제 5(겹친 측정줄·XY 붙음·밝은 패드 오인식)와
과제 6(JPG 지원)도 해결됨 (2026-07-28)** — `Sample2`/`Sample3` 실제 문제 사진으로 재현·검증.
남은 건 과제 3의 선택적 추가 검증(②③④) 정도로 급하지 않음.

**중요 — 확인 필요:** 과제 5의 OCR 핵심 로직 변경(`ocr_core.py`의 글자줄 묶기·형식 검사 완화 등)
이후로 `samples/Sample` 기준 회귀 테스트를 아직 안 돌려본 상태(`실행이력.csv`가 로컬에도
git에도 없음 — 로깅 기능을 넣은 뒤로 프로그램이 한 번도 실행된 적이 없다는 뜻). CLAUDE.md
규칙 4번대로 **다음 작업 시작 전에 `samples/Sample`로 한 번 돌려서 기준값
(`5.5 Space Avg=5.3923 / 9.5 Line Avg=9.4154 / 14.5 Pad CD Avg=14.9538`, 측정결과 시트 92행)이
그대로인지 반드시 확인할 것.**

사용자는 초보이니, 큰 걸 한 번에 만들지 말고 **작게 만들어 보여주고
확인받으며** 진행할 것. 그리고 **응답마다 빠짐없이 `scripts/notify-toast.ps1` 알림을 보낼 것**
(사용자가 여러 번 강조한 규칙).

---

## 8. 웹 버전 (`webapp/`) — 신규, 2026-07-30~31

데스크탑 프로그램(tkinter)과 별개로, **사내 여러 명이 브라우저로 접속해서 쓸 수 있는
웹 버전**을 새로 만들기 시작함. 요구사항(사용자가 직접 정리): ①외부 링크 접속,
②인원 통제(회원가입 아닌 계정 관리), ③실행 로그를 웹에서 확인 가능, ④(나중에) 포트폴리오용.

### 구조
- **기존 데스크탑 코드를 그대로 재사용** — `CD측정값_엑셀변환.py`(process_folder 등),
  `ocr_core.py`, `measurement_plan.py`, `tolerance_table.py`, `html_report.py`를
  `webapp/app.py`에서 import해서 씀. 데스크탑/웹이 다른 결과를 낼 위험을 없애기 위해
  로직을 중복 작성하지 않음.
- `process_folder`에 **`progress_cb` 파라미터 추가**(선택적, 데스크탑은 안 넘기므로
  영향 없음) — 사진 한 장 읽을 때마다 호출돼서 웹의 진행률 표시줄에 씀.
- `CD_MEASURE_DATA_DIR` 환경변수를 새로 둬서, 실행이력.csv·계정·업로드/결과 파일이
  전부 이 폴더 밑에 모이게 함(Docker 배포 시 `/data`로 지정, 볼륨으로 밖에 연결 —
  컨테이너를 다시 만들어도 안 사라지게 하려는 목적). 환경변수 없으면 데스크탑처럼
  프로그램 폴더 기준으로 그대로 동작(하위호환).

### 구현된 기능
- **로그인 (`webapp/users.py`, `add_user.py`)**: 회원가입 화면 없음 — 관리자가
  `docker exec ... python add_user.py add 아이디`로 직접 계정 생성. 비밀번호는
  해시로만 저장(`werkzeug.security`), `/login` 빼고 전 페이지 `@login_required`.
- **새 측정 폼 (`new_run.html`)**: 웹판 `plan_gui.py` — Item 개수 입력 후 "생성" 누르면
  JS로 그만큼 입력칸 생성(tkinter의 `ItemListSection._build_rows`와 같은 방식).
  **사진은 `webkitdirectory` 속성으로 폴더째 선택**(파일 하나하나 고르는 방식이 아님 —
  데스크탑과 같은 사용 경험을 위해).
- **백그라운드 처리 + 실시간 진행률**: 업로드 즉시 백그라운드 스레드에서 처리 시작,
  화면은 `processing.html`에서 1초마다 `/progress/<id>`를 폴링해 "N/전체 (%)" 표시줄
  갱신. 진행 상태는 `.progress.json` 파일에 저장(gunicorn 워커가 여러 개라 메모리로는
  공유 안 되므로, 실행 폴더 안 파일로 공유).
- **결과 화면 (`result.html`)**: 확인필요/스펙이탈/겹침 개수 요약, 엑셀/HTML 다운로드,
  **"원본 사진 삭제" 버튼**(확인 대화상자 포함) — 외부 서버에 민감한 원본 이미지를
  계속 안 남겨두려는 목적. 확인필요 값 검증 워크플로를 위해 자동삭제는 안 하고 사용자가
  직접 눌러야 지워짐. 엑셀/HTML/로그는 안 지워짐.
- **실행 기록/로그 조회 (`/runs`, `/log/<id>`)**: 요구사항 ③ 대응 — SSH 없이도 웹에서
  지난 실행(성공/처리중/**실패** 전부) 목록과 상세 로그(사진별 처리 내역, 실패 시
  원인+traceback)를 볼 수 있음. 예전엔 처리 실패하면 흔적이 아예 안 남았는데,
  `_write_failure_log`로 실패해도 로그 파일을 남기도록 고침.

### Docker 배포
- `webapp/Dockerfile` — `python:3.12-slim` 기반. 기존 코드가 최상단에서 tkinter를
  불러오므로(`plan_gui.py`의 상수 하나만 쓰지만 import 자체에 필요) `apt-get install
  python3-tk` 필요(pip로는 안 깔림, 시스템 패키지). gunicorn `-w 4 --timeout 900`으로
  운영(워커 4개 = 사내 5명 정도 동시 사용 고려, timeout은 사진 많을 때 대비 — 실측
  사진 1장당 약 1초).
- `.dockerignore` — `samples/`(500MB+ 사진), `webapp/users.json`(비밀번호 해시),
  `webapp/uploads/` 등 이미지에 안 들어가게 제외.
- `webapp/배포방법.md` — 실제 배포 명령어(윈도우/리눅스), 계정 생성법, 이미지 업데이트
  절차, 문제 해결표를 정리한 별도 문서.
- **로컬 검증 완료 (2026-07-30)**: 이 컴퓨터에 Docker Desktop + WSL2 새로 설치,
  로컬 컨테이너로 로그인~폴더업로드~진행률표시~처리~결과~다운로드~원본사진삭제~
  실행기록/로그조회까지 Playwright(헤드리스 브라우저)로 전부 실제 클릭해서 검증함.
  데스크탑 회귀 테스트(`samples/Sample` 기준값 5.3923/9.4154/14.9538)도 그대로 유지됨.

### 배포 위치 — 외부 클라우드로 결정 (2026-07-31)
처음엔 "사내망 전용"으로 시작했으나(공인 IP로 인터넷에 안 열기로 2026-07-30 합의),
사용자 개인 PC를 임시 서버로 써서 동료 PC 접속을 시도한 결과 **타임아웃으로 실패** —
회사 네트워크가 직원 단말기끼리의 통신을 막는 "클라이언트 격리" 정책을 쓰는 것으로
추정됨(방화벽 규칙은 정상 등록됐는데도 안 됨, 진단 결과). 그래서 진짜 서버가 필요해짐.

이 시점에 사용자가 "외부 클라우드로 구축하자"고 제안 → Claude가 confidential 데이터
(고객사명 등) 외부 노출 우려를 제기 → **사용자가 "이미 승인된 사항"이라고 명시적으로
확인**(개인 명의 무료 계정 사용도 포함해서 재확인함) → 그 결정을 존중해 진행하기로 함.
(이 PC를 임시 서버로 쓰려고 5000번 포트를 열었던 방화벽 규칙 "CD Measure Web"은
2026-07-31에 다시 제거함 — 더 이상 필요 없어짐.)
단, **HTTPS는 승인 여부와 무관하게 필수**로 요구함(평문으로 비밀번호가 오가면 안 되므로)
— **구현 완료(2026-07-31)**, 아래 참고.

**선택한 클라우드: Oracle Cloud "Always Free" 티어** (평생 무료, VM 상시 가동 가능).
- **홈 리전: Japan Central (Osaka)** — 가입 화면에 서울 리전이 안 보였고, 오사카가
  한국에서 지연시간도 나쁘지 않으면서 무료 ARM 인스턴스 재고 확보도 서울보다 수월하다는
  판단(커뮤니티 후기 기준)으로 선택. **홈 리전은 나중에 못 바꿈**(재가입해야 함).
- VCN "cd-measure-vcn"을 **"Create VCN with internet connectivity" 마법사**로 생성
  완료(Public/Private 서브넷, Internet Gateway, NAT Gateway, Service Gateway,
  Security List, Route Table 전부 자동 구성됨). ⚠️ 콘솔의 "인스턴스 생성 화면 안에서
  즉석으로 VCN 만들기" 방식은 실제로 버그성으로 Public IP 배정이 막혀서 실패했음 —
  Networking 페이지에서 VCN을 먼저 따로 만드는 방식으로 우회해서 해결함.

### 현재 막힌 지점: Ampere A1.Flex "Out of capacity" (2026-07-31~08-01, 진행 중)

> **2026-08-02 현재 상태 한 줄 요약**: ARM 인스턴스는 **아직 못 잡음**. 오라클 AMD
> 무료 서버(`cd-measure-amd-1`)에서 재시도 루프가 24시간 자동으로 돌고 있고,
> 확보되면 디스코드로 알림이 옴. 개인 컴퓨터는 꺼도 됨.
> **다음 행동: 8/3(월) 회사 PC 루프 종료 + Oracle 계정 PAYG 전환.**
> 아래는 여기까지 온 과정.

**2026-08-03(월) 진행 상황**
- 세션 시작하며 서버(`cd-measure-amd-1`)의 재시도 systemd 서비스가 **약 6시간째 멈춰
  있었던 것 발견**(감사 로그에 LaunchInstance 0건). 원격 진단 시도했으나 막힘 —
  SSH는 집 PC 키만 등록돼 있어 회사 PC 키로 접속 불가, "Run Command"(SSH 없이
  원격 명령 실행)로 우회하려 했으나 **이 인스턴스엔 그 에이전트 플러그인 자체가
  비활성 상태**라 실패. 서버 자체 진단은 미해결로 남음(SSH 키 추가 등록 또는
  Oracle Cloud Shell 등으로 나중에 접근 필요).
- 근본 목표(ARM 재시도 지속)를 위해 **회사 PC에서 공용 스크립트를 세션-독립
  프로세스로 재가동**(`Start-Process ... -WindowStyle Minimized`, 지난 세션 종료 시
  이전 재시도가 같이 죽는 걸 겪은 뒤 채택한 방식).
- **Oracle 계정 PAYG 전환 완료**(카드 등록 시 검증 목적의 임시 승인·자동 취소 있었음,
  정상). 이후 재시도는 아직도 재고 부족으로 대기 중(전환 직후 바로 잡히진 않음).
- ⚠️ **중요 정정**: "무료 한도 4 OCPU/24GB"는 틀린 정보였음. Oracle이 2026-06-15부터
  공지 없이 **2 OCPU/12GB로 축소**함(웹 검색으로 확인). 이 문서 여러 곳의 "4 OCPU/24GB"
  표기를 정정 표시함 — **PAYG 전환 후 사양 상향은 2 OCPU/12GB까지만 무료**, 그 이상은
  과금됨.
- **ARM 확보됐으나 동시에 3대가 생기는 경합(race condition) 발생.** PAYG 전환 후
  오사카 리전에 재고가 풀리자, 서로 모르는 여러 재시도 프로세스(회사 PC + 어딘가에
  살아있던 것으로 보이는 옛날 이름 패턴의 프로세스)가 05:25~05:27(UTC) 사이에 각자
  성공해버림: `cd-measure-server`(2 OCPU/12GB), `cd-measure-web`×2(각 1 OCPU/6GB).
  합계 4 OCPU/24GB로 무료 한도(2 OCPU/12GB)의 2배 — PAYG라 초과분이 과금될 상황.
  사용자 확인 후 `cd-measure-server`(2 OCPU/12GB, 무료 한도를 정확히 채우는 구성)만
  남기고 `cd-measure-web` 2대는 부트볼륨까지 함께 종료(`--preserve-boot-volume
  false`). 짧게(생성~종료 약 10분) 켜져 있었을 뿐이라 실제 초과 과금은 몇 센트
  수준으로 추정(정확한 금액은 콘솔 Cost Analysis 반영 지연으로 즉시 확인 불가).
  → **회사 PC 쪽 재시도 프로세스는 이미 스스로 종료된 상태(목표 달성 후 자연 종료로
  추정)라 추가 위험 없음.** 다만 `cd-measure-server`라는 이름은 세션 아주 초반에
  썼던 옛날 bash 재시도 스크립트의 이름 패턴이라, **그 스크립트가 어딘가(서버쪽?)에
  아직 살아있을 가능성**이 있고, 확인을 위해 `cd-measure-amd-1`에
  SSH 접속을 시도했으나 실패 → 아래 항목에서 원인이 밝혀짐.
- 🔴 **오늘의 가장 큰 발견: 회사 네트워크가 아웃바운드 SSH(22번)를 차단하고 있었음.**
  오라클 서버 3대 모두 22번이 안 되길래 처음엔 ①"SSH 키가 안 맞아서" ②"서버 내부
  방화벽(ufw/iptables) 때문에"라고 진단했는데 **둘 다 틀렸음**. 결정적 증거는 오라클과
  무관한 GitHub 테스트 — `github.com:22`는 차단, `github.com:443`은 정상. 즉 서버는
  전부 멀쩡하고 원인은 회사망 하나였음.
  - 검증 과정에서 확인된 사실들:
    - 회사 PC 키는 개인키·공개키 짝이 맞고(`SHA256:LbWsHRu+PeXX...`), 그 공개키가
      `cd-measure-server`에 정확히 등록돼 있음 → **키 문제 아님**
    - Security List(0.0.0.0/0 허용), NSG(없음), 라우트 테이블(IGW 연결),
      인터넷 게이트웨이(활성) 전부 정상
    - **시리얼 콘솔은 접속 성공** — 이건 22번이 아니라 **443번**을 쓰기 때문.
      `cd-measure-server login:` 프롬프트까지 도달해 서버가 살아있음을 직접 확인.
      단, 클라우드 이미지는 OS 비밀번호가 없어 로그인은 못 함.
      (콘솔 연결 키는 **RSA만 허용** — ed25519는 거부당함. 또 요즘 OpenSSH는 옛 방식
      호스트키를 기본 거부하므로 `-o HostKeyAlgorithms=+ssh-rsa` 필요.)
    - 비밀번호를 심으려 cloud-init(`user_data`) 주입을 시도했으나, **Oracle은 인스턴스
      생성 후 `user_data`·`ssh_authorized_keys` 변경을 API에서 원천 거부**함.
      → 나중에 키를 추가 등록하는 건 불가능. 서버에 한 번 들어가서
      `~/.ssh/authorized_keys`를 직접 고치는 수밖에 없음.
  - **결론: 배포는 집 PC에서 진행.** 22번을 443번으로 감싸 우회하는 방법이 있긴 하나
    회사 보안 통제를 뚫는 행위라 하지 않기로 함. 집에서 할 구체적 절차는
    `집에서_이어서_작업하기.md` 맨 위 "🔴 지금 집에서 할 일" 참고.
  - ⚠️ 주의: `cd-measure-server`에는 **회사 PC 키만** 심겨 있음(집 PC 키 아님).
    이 인스턴스는 `oracle_retry_launch.py`(ARM 이름을 `cd-measure-web`으로 붙임)가
    아니라 **회사 PC의 옛날 bash 스크립트**가 만든 것으로 보임 — 이름 패턴과 키
    이름표(`cd-measure-server`)가 둘 다 그쪽과 일치함. 따라서 집에서 접속하려면
    회사 PC 개인키를 USB로 옮겨와야 함.

**주말 경과 (2026-08-02 14:00 기준)**
- 서버 루프는 **1일 3시간 30분 무중단 가동** 중(서버 자체는 재부팅 없음, load 0.05).
- 8/2 06:19·06:25에 우분투 자동 보안 업데이트(`unattended-upgrades`)가 서비스를
  재시작시켰지만 **systemd가 즉시 되살려 공백은 약 7분**이었음. 개인 컴퓨터였다면
  그대로 멈췄을 상황(8/1 새벽에 실제로 3시간 손실)이라, 서버로 옮긴 판단이 맞았음.
- **감사 로그 누적 집계(7/31 22:07~8/2 14:03): LaunchInstance 1,310건, ARM 성공 0건.**
  출처별로 회사 PC 806건(62%, 지금도 가동 중) / AMD 서버 412건 / 집 PC 90건
  (8/1 10:37에 정리) + AMD 인스턴스 생성 성공 2건.
  ※ 429(요청 제한)로 거부된 요청은 감사 로그에 안 남으므로 실제 시도는 이보다 많음.
  ※ 출처 구분은 감사 로그의 접속 IP로 함(API 키 지문은 오라클이 가려서 안 보임).
- 이틀 넘게 1,300회 이상 시도해도 재고가 한 번도 안 돈 것이므로, **오사카 ARM 재고가
  마른 상태라는 판단은 확정적**. PAYG 전환이 남은 카드.
무료 ARM 인스턴스(`VM.Standard.A1.Flex`, OCR용 numpy/scipy/pandas 감안해 이걸 고름)가
인기가 많아서 재고 부족 에러가 반복됨 — Oracle Free Tier에서 흔히 겪는 문제(커뮤니티에서
자동 재시도 스크립트를 쓰는 게 사실상 표준 우회법으로 자리잡음).

**대응**: OCI CLI 설치 + API 서명 키 생성(`~/.oci/config`, `~/.oci/oci_api_key.pem` —
**이 컴퓨터에만 있음, git 추적 안 됨**) 완료. 필요한 리소스(가용성 도메인, public
서브넷, Ubuntu 24.04 ARM 이미지 OCID)를 CLI로 조회해서 확보. **4분마다 자동으로
`oci compute instance launch`를 재시도하는 스크립트**를 백그라운드에서 돌리는 중 —
"재고 부족"이나 "429 요청 제한" 에러면 조용히 재시도, 성공하면 Windows 알림 + 자동 정지,
그 외 진짜 오류면 알림으로 즉시 알려줌. 인스턴스용 SSH 키도 이때 새로 생성해둠
(`~/.ssh/cd_measure_oracle`, 콘솔에서 다운로드한 키와는 별개 — CLI 재시도용으로 새로 만듦).

**집 컴퓨터에서도 재시도 시작 (2026-07-31 밤)** — 회사 PC 스크립트가 git에 없어서
이어받을 수 없었던 문제를 정리하면서, 이번엔 스크립트를 커밋함
(`scripts/oracle_retry_launch.py`, 양쪽 컴퓨터 공용).
- **CLI가 아니라 파이썬 SDK(`oci`)로 구현**: 집 컴퓨터에서 `pip install oci-cli`가
  Windows 260자 경로 제한으로 실패함(oci-cli 안의 도움말 파일 경로가 너무 김).
  파이썬 SDK는 정상 설치돼서, 관리자 권한·추가 설치 없이 같은 일을 할 수 있는 SDK
  방식으로 만듦. 기존 프로젝트 패키지(numpy/scipy/pandas/openpyxl/pillow) 버전은
  설치 전후 그대로임을 확인함.
- **OCID 하드코딩 없이 자동 조회**: VCN 이름(`cd-measure-vcn`) → public 서브넷 →
  Ubuntu 24.04 ARM 최신 이미지. 계정마다 값이 달라 git에 올리기 애매하고, 콘솔에서
  뭘 다시 만들면 값이 바뀌기 때문.
- **시작 전 기존 인스턴스 확인** → 다른 컴퓨터가 먼저 성공했으면 중복 생성하지 않고
  IP만 알려주고 종료(무료 한도 초과 방지).
- **API 키는 컴퓨터마다 따로**: 집 컴퓨터용 키를 새로 만들어 콘솔에 **추가 등록**
  (지문 `59:f0:...`, 회사 PC 키 `9a:7e:...`는 그대로 유지 — 지우면 회사 루프가 죽음).
  등록 직후 401이 간헐적으로 섞여 나옴(실측 12번 중 1번, 전파 지연) → 401 5회 연속일
  때만 중단하도록 처리.
- **감사 로그로 확인한 사실**: 회사 PC는 11:34~23:07 사이 `LaunchInstance`를 **176번
  시도해 전부 500(재고 부족)**. 감사 로그에 요청 본문은 안 남아서 회사 PC가 쓴
  OCPU/메모리 값은 복원 불가. 한 조합만 계속 두드리는 게 효과가 없다는 근거라,
  집 루프는 **4:24 → 4:12 → 2:24를 번갈아** 요청함(전부 무료 한도 4 OCPU/24GB 안,
  A1.Flex는 OCPU당 1~64GB 허용이라 셋 다 유효). 그래도 안 잡히면 `--shapes 1:6`처럼
  더 작게 줄이는 게 확률이 가장 높음.
- 밤새 돌려야 하므로 Claude Code 세션에 묶이지 않는 **독립 프로세스**로 실행
  (`Start-Process python scripts\oracle_retry_launch.py -WindowStyle Minimized`).
  진행 상황은 `scripts/oracle_retry.log`(gitignore 대상).

**밤새 결과 + 주말 계획 (2026-08-01 아침)**
- 집 루프는 07-31 23:16 ~ 08-01 06:02에 102번 시도 후, **Windows 자동 업데이트
  재부팅(06:10)으로 죽어** 3시간을 놓쳤음. → 로그인 시 자동으로 다시 뜨도록
  시작 폴더에 `cd-measure-oracle-retry.cmd`를 등록해 재발 방지(배포 끝나면 그 파일만
  지우면 됨). 스크립트가 시작할 때 기존 인스턴스를 확인하므로 중복 생성 위험 없음.
- **감사 로그 집계: 최근 22시간 LaunchInstance 431건 전부 500(재고 부족), 성공 0건.**
  회사 PC 루프도 계속 살아 있었고(시간당 20건 ≈ 3분 간격) 역시 못 잡음. 조합
  (4:24 / 4:12 / 2:24)을 바꿔도 결과가 같아서, 오사카 ARM 재고 자체가 마른 상태로 판단.
- **결정: 주말(8/1~8/2) 동안 계속 재시도하고, 그래도 안 되면 8/3(월)에 Oracle 계정을
  Pay As You Go로 전환**해서 다시 시도한다(무료 계정보다 A1 재고 우선순위가 높다는 게
  커뮤니티 공통 보고). 전환해도 Always Free 자원은 계속 무료지만, **무료 한도를 넘기면
  과금되므로 전환 후에는 인스턴스 사양·개수를 무료 한도(당시 파악: ARM 4 OCPU/24GB,
  부트볼륨 합계 200GB) 안으로 유지해야 함.**
  > ⚠️ **정정 (2026-08-03)**: 이 문서 전체에서 "무료 한도 4 OCPU/24GB"라고 적은 부분은
  > **틀린 정보**였음이 드러남. Oracle이 **2026-06-15부터 공지 없이 무료 한도를
  > 4 OCPU/24GB → **2 OCPU/12GB**로 축소**했음(웹 검색으로 확인, 여러 매체가 보도:
  > "Oracle Quietly Cuts Free Tier Ampere A1 Resources in Half"). 8/1 시점엔 이 사실을
  > 몰랐던 채로 작성됨. **PAYG 전환 후 사양을 올릴 땐 2 OCPU/12GB까지만 무료이고, 그
  > 이상은 실제로 과금됨** — 아래 "ARM을 확보하면 다음 순서" 2번도 이 기준으로 정정함.
- 참고: 컴퓨터가 절전/최대절전으로 들어가면 재시도도 멈춤 — 주말엔 켜둔 상태를 유지할 것.

**AMD 무료 인스턴스 2대 확보 + 재시도 루프를 클라우드로 이전 (2026-08-01 10:26~10:39)**
- **AMD `VM.Standard.E2.1.Micro` 2대 확보 성공, 3분 소요** — ARM과 달리 재고가 넉넉함.
  `cd-measure-amd-1`, `cd-measure-amd-2`(점프 서버), Ubuntu 24.04 x86_64.
  실제 IP는 공개 저장소에 나가면 안 되므로 비공개 문서(`집에서_이어서_작업하기.md`)에만 적어둠.
- **ARM과 AMD는 무료 할당량이 완전히 별개**임을 계정에서 직접 확인
  (`standard-e2-micro-core-count`=2, `standard-a1-core-count`는 그대로). 그래서 AMD를
  확보해도 ARM 몫(당시 파악 4 OCPU/24GB, 2026-08-03 정정: 실제로는 2 OCPU/12GB — 아래
  참고)을 받을 권리는 남아 있음. **단 E2.1.Micro는 고정 사양이라
  사양을 못 올림** — 본 웹앱(OCR+gunicorn)은 1GB로는 무리이므로 ARM에 올려야 함.
  AMD는 보조 서버(재시도 루프 실행, 나중에 리버스 프록시) 용도.
  - ⚠️ 이때 잡은 버그: 기존 재시도 스크립트는 "인스턴스가 하나라도 있으면 종료"였는데,
    그대로 두면 AMD를 확보한 순간 ARM 재시도가 스스로 멈춰 **ARM을 영원히 못 받게 됨**.
    같은 사양만 세도록 고침(`same_shape_instances`).
- **재시도 루프를 `cd-measure-amd-1`에서 24시간 돌리도록 이전.** 개인 컴퓨터는 절전·
  Windows 자동 재부팅·외출 때 멈추는데(실제로 3시간 놓쳤음) 클라우드 서버는 안 꺼짐.
  - 인증은 **인스턴스 프린시펄** 방식 — 서버에 API 개인키를 두지 않음. 서버가 뚫려도
    계정 전체가 넘어가지 않게 하려는 목적. 필요한 동적 그룹·정책은
    `scripts/oracle_grant_instance_permission.py`가 만듦(권한은 인스턴스 생성 + 네트워크·
    이미지 조회로 한정, 과금·사용자·스토리지는 못 건드림).
    - 정책 전파에 몇 분 걸림. `read virtual-network-family`를 빼먹으면 `list_vcns`가
      404 `NotAuthorizedOrNotFound`로 실패함(실제로 겪고 추가함).
  - 서버에서는 `systemd` 서비스 `cd-measure-retry`로 상시 실행(재부팅에도 자동 시작,
    `Restart=on-failure`). 로그는 서버의 `/home/ubuntu/oracle_retry.log`, 시간대는 KST로 맞춤.
  - 이전 완료 후 **집 PC 루프와 시작 폴더 자동시작(`cd-measure-oracle-retry.cmd`)은 제거**
    — 같은 대기줄에 중복 요청만 늘리므로. 이제 개인 컴퓨터를 켜둘 필요가 없음.
    (회사 PC 루프는 아직 돌고 있어서 월요일에 정리 필요.)
  - ⚠️ 서버에서는 Windows 알림을 띄울 수 없음(`notify-toast.ps1`이 없어 자동으로 건너뜀).
    확보 여부는 집에서 `python scripts/oracle_retry_launch.py --status`로 확인하거나
    서버 로그를 보는 방식.

**전략 변경: 작은 사양으로 일단 확보 (2026-08-01 오전)**
큰 사양 조합이 431번 전부 실패한 뒤, **가장 작은 1 OCPU/6GB만 노리도록** 바꿈
(`DEFAULT_SHAPES`). A1.Flex는 확보 후 콘솔에서 무료 한도(2026-08-03 정정: **2 OCPU/12GB**,
당시엔 4 OCPU/24GB로 잘못 알고 있었음)까지 올릴 수 있으므로(재부팅 필요) "서버를 먼저
갖고 나중에 키운다"가 합리적. 6GB로도 웹앱
(gunicorn 4워커 + OCR)은 충분하지만, **1 OCPU는 사진 처리가 CPU를 오래 쓰는 작업이라
여러 명이 동시에 올리면 느려질 수 있음** → 확보되면 사양 상향을 시도할 것.
- 이 판단의 참고자료로 사용자가 네이버 블로그 글(오라클 무료티어 ARM 확보 팁)을
  가져옴. 쓸 만한 부분(작은 사양 우선, 오사카는 PAYG면 거의 즉시 생성된다는 언급,
  **콘솔 Security List와 서버 iptables를 둘 다 열어야 한다는 점**, 스왑 설정, 생성 직후
  SSH 안 돼도 지우지 말 것)은 반영함 — 포트/스왑은 `webapp/배포방법.md` 2-A-2·2-A-3에 정리.
  근거 없는 주장(숨겨진 예약 용량 풀, 콘솔보다 API가 검증이 느슨함, 화·수 새벽 4~6시
  골든타임)은 채택하지 않음. 특히 "새벽 골든타임"은 우리 감사 로그로 반증됨
  (04시 28건·05시 27건 전부 재고 부족).

**ARM을 확보하면 다음 순서** (2026-08-01 갱신):
1. 디스코드 알림으로 공인 IP 확인 (SSH 키는 이미 심겨 있어 바로 접속 가능:
   `ssh -i ~/.ssh/cd_measure_oracle ubuntu@<IP>`)
2. **콘솔에서 사양 상향 시도** (1 OCPU/6GB → **무료 한도 2 OCPU/12GB까지만**, 재부팅
   필요. ⚠️ 2026-08-03 정정: 예전엔 4 OCPU/24GB가 무료인 줄 알았으나, Oracle이
   2026-06-15부터 공지 없이 한도를 절반으로 줄임 — **2 OCPU/12GB를 넘기면 PAYG
   전환 이후이므로 실제 과금됨**. 1 OCPU로는 사진 처리가 여러 명 동시 사용 때 느릴
   수 있음. 상향도 재고가 필요하므로 안 되면 그냥 쓰다가 나중에 재시도.
3. **80/443 포트를 두 군데 열기** — 콘솔 Security List 인그레스 + 서버 OS의 iptables.
   (`webapp/배포방법.md` 2-A-2. 이거 빼먹으면 "연결 시간 초과"로 몇 시간 헤맴)
4. 서버에 Docker 설치 + **스왑 4GB 설정**(2-A-3)
5. DuckDNS 도메인의 IP를 이 서버 IP로 변경 → `.env` 작성 → `docker compose up -d --build`
6. 팀원 계정 생성(`add_user.py`) → 실제 인터넷에서 접속 검증(Let's Encrypt 발급 확인)
7. 배포 끝나면 AMD 서버의 재시도 서비스 정리: `sudo systemctl disable --now cd-measure-retry`

### HTTPS — 구현 완료 (2026-07-31, 로컬 검증됨)
Caddy(자동 HTTPS 리버스 프록시) + `docker-compose.yml`로 구성. 프로젝트 최상위의
`Caddyfile`·`docker-compose.yml` 참고. `CD_MEASURE_DOMAIN` 환경변수로 실제 도메인
(DuckDNS 등)을 넣으면 Caddy가 Let's Encrypt 인증서를 자동 발급/갱신함(certbot
설정 불필요). 로그인 브루트포스 방어가 Caddy 뒤에서도 접속자별 IP를 정확히
구분하도록 `CD_MEASURE_TRUST_PROXY=1` + `ProxyFix(x_for=1, x_proto=1)` 적용.
**로컬(`https://localhost`, 도메인 없이 자체서명 인증서)로 HTTP→HTTPS 자동
리다이렉트와 실제 로그인까지 브라우저로 검증 완료** — 다만 진짜 도메인으로
Let's Encrypt 인증서를 받는 것과 실서버 환경 자체는 Oracle 인스턴스가 아직 없어
미검증. 자세한 명령어는 `webapp/배포방법.md` 2번 섹션.

### 로그인 무차별 대입 방어 — 구현 완료 (2026-07-31)
같은 IP가 5분 안에 5번 로그인 실패하면 15분간 잠금(맞는 비밀번호를 넣어도 그동안
막힘). 상태는 gunicorn 다중 워커에서도 공유되도록 파일(`login_attempts.json`)에
저장. Playwright로 5회 실패 → 잠김 → 잠긴 동안 올바른 비밀번호도 거부되는 것까지
검증함(`webapp/app.py`).

### 아직 안 된 것 (다음 작업 후보)
- ~~**ARM 인스턴스 확보**~~ → 확보 완료. 지금 실서버가 그 위에서 돎
  (Oracle Cloud ARM + Docker Compose + Caddy).
- **서버(`cd-measure-amd-1`) 재시도 서비스 원격 진단** — 8/3 발견한 문제, 미해결.
  약 6시간 멈췄던 원인을 못 봄(SSH 접속 불가, Run Command 플러그인도 비활성).
  회사 PC 키를 서버에 추가 등록하거나(집 PC로 SSH 접속해서 authorized_keys에 추가,
  또는 Oracle Cloud Shell 이용), Run Command 플러그인을 콘솔에서 켜는 방법을
  시도해볼 것. 지금은 회사 PC 쪽 재시도로 대체해서 급하지 않음.
- **집 컴퓨터에 Docker 미설치** — 웹앱을 집에서 로컬로 테스트하려면 필요함
  (WSL2 활성화 → `winget install --id Docker.DockerDesktop`, 둘 다 관리자 권한 + 재부팅).
  2026-08-01에 시도했으나 UAC 창이 취소돼서 보류. **실서버 배포에는 불필요**(서버에는
  리눅스용 Docker를 따로 설치).
- ~~**④ 포트폴리오용 데모 모드**~~ → **완료(2026-08-18~20). 아래 9절 참고.**
- 계정 관리 웹 화면(지금은 SSH로 `add_user.py` 실행해야 함), 오래된 데이터 자동 정리,
  자동 로그아웃, `실행이력.csv`/`users.json` 백업 — 전부 "있으면 좋음" 수준, 급하지 않음.

---

## 9. 공개 데모와 케이스 스터디 (2026-08-18~20)

8절의 요구사항 ④(포트폴리오용)를 실제로 만든 것. **실제 fab 데이터를 한 장도
쓰지 않고** 도구 전체를 보여주는 것이 목표였음.

### 9-1. 계측기 사진을 코드로 그림 (`scripts/make_demo_images.py`)

실사진은 사내 데이터라 밖에 못 내놓으므로 **사진 자체를 그렸음**. 실제 사진을
픽셀 단위로 관찰해 재현함.

- 실제 사진은 SEM이 아니라 **광학 현미경 사진** — 전체적으로 흐릿하고 대비가 아주
  낮음(전 채널 80~150). 색 상수는 전부 실측값.
- 측정 표시는 십자 마커가 아니라 **선분 + 양 끝 X**이고, **전부 초록으로 이어 그려야**
  함. `ls_brightness`가 초록 덩어리의 *중심* 주변 밝기를 재기 때문 — X만 따로 찍으면
  중심이 경계에 놓여 판정이 무너짐.
- **배경은 어느 채널도 170을 넘으면 안 됨** — `get_white_mask`가 `R>170 & G>170 & B>170`
  을 글자로 보기 때문. 넘으면 배경 무늬가 글자로 잡혀 줄 개수 판별이 틀어짐.
- 난수 seed 고정이라 어느 컴퓨터에서 만들어도 같은 36장이 나옴. 만든 뒤 **실제
  판독기로 다시 읽어** 확인필요 0건인지 자체 점검함.

이 사진들이 **6번째 회귀 기준값**이 됐음(`regression_check.py --demo`). 실사진에만
묶여 있던 회귀 검증을 공개 저장소에서도 돌릴 수 있게 된 것.

### 9-2. 공개 데모 (`/demo`, 무로그인)

- 무로그인 라우트 6개: `/demo`, `/demo/start`, `/demo/processing`, `/demo/progress`,
  `/demo/result`, `/demo/download`, `/demo/photo`. IP당 하루 5회 제한.
- 접근 제어의 핵심은 `_is_demo_run_id()` — `demo_` 접두사 + `secure_filename()` 왕복
  불변성. 실행 데이터가 무로그인 경로로 새지 않게 하는 유일한 방어선.
- 데모 사진 폴더는 첫 접속 때 한 번 만들고(`_ensure_demo_source`) `/data` 볼륨에 남김.
  **원자적으로 만들어야 함** — 중간에 끊기면 반쪽 폴더가 영구 고착됨(폴더가 있으면
  그냥 반환하므로 다시 만들 기회가 없음).

### 9-3. 판독 과정을 눈에 보이게 (2026-08-19)

데모가 결과 숫자만 보여줘서 **방문자가 무슨 사진이 처리되는지 몰랐음.** 이 도구의
값어치는 "사진 → 숫자"인데 정작 그 변환이 화면에 없었던 것.

- 사진 위에 **SVG 층을 겹쳐** 주석을 그림. 픽셀에 굽지 않는 이유는 문구를 고칠 때마다
  사진을 다시 만들어야 하기 때문. `viewBox`를 원본 크기(1280×1024)에 맞추면 원본 픽셀
  좌표를 그대로 쓰면서 문구는 텍스트로 남음.
- 결과 화면은 측정 지점별로 **실제로 읽은 영역을 초록 사각형**으로 표시하고 값·규격
  판정을 나란히 보여줌. 좌표는 미리보기를 만들 때 `crop_boxes()`에서 공짜로 나옴.
- ⚠️ **그림은 공유하고 값은 실행별로 둠.** 사진이 고정이라 값도 고정이지만, 미리
  계산해 넣으면 **판독이 깨져도 화면은 멀쩡해 보임.** 값은 그 실행의 결과
  (`사진별판독.json`)에서만 가져옴.

### 9-4. 케이스 스터디 (`/about`, 무로그인)

설명하는 글이 README·케이스 스터디·아티팩트 세 군데로 갈라져 어긋났음.
**원본은 `docs/case-study.md` 하나**로 두고 `scripts/build_case_study.py`가 배포 전에
사이트 화면으로 변환함(서버에 markdown 의존성을 늘리지 않으려고 빌드 시점에 변환).
저장소 상대 링크·이미지는 사이트 주소로 치환하고, 못 바꾼 것이 남으면 빌드를 실패시킴.

### 9-5. 공개 저장소 (`CD-Measure-public`)

Private 저장소에서 **한 방향으로만** 내보냄(`scripts/export_public.py`).

- **허용목록 방식** — "뺄 것"이 아니라 "넣을 것"만 적음. 새 파일이 실수로 새어나갈 수
  없게 하려는 것. 한 겹 더 얹어 **git 추적 파일만** 복사함.
- 사진은 원칙적으로 금지, `IMAGE_WHITELIST`에 적은 **합성 이미지만** 예외.
  허용목록은 **경로 단위**라 같은 파일이어도 경로가 다르면 다시 걸림(실제로 두 번 막혔음).
- 커밋·푸시는 일부러 안 함 — 공개는 되돌리기 어려우므로 사람이 파일 목록을 눈으로
  본 뒤 직접 함.

### 9-6. 화면 문구 기준

사용자 리뷰로 정해진 기준. 새 문구를 넣을 때마다 지킬 것.

1. 설명 문장은 **-습니다체**(해요체 금지)
2. 요청은 **'-해 주세요'**(표준 띄어쓰기)
3. **Target·Point·Item은 화면에서 처음 한 번만 병기**하고 이후 한국어로
   (목표값 / 측정 지점 / 항목)
4. 계측 용어: 스펙 → **규격**, 틀어진 정도 → **정렬 오차**, 선의 두께 → **선폭**,
   틈의 두께 → **간격**
5. '사진'은 현장 용어라 유지하고, 데모의 합성물만 **예시 이미지**로 구분
